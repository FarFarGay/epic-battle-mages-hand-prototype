# EBM — Дизайн-ядро

**Что это:** чистый дизайн-документ игры. Только то, что ЖИВЁТ в коде, + целевое видение (скелет Dome Keeper × наши особенности). Без легаси.
**Чем НЕ является:** не замена `SPEC.md` (низкоуровневая реализация, 6к строк, ниже строки 3217 — легаси) и не `docs/gameplay.md` (игрок-фейсинг туториала, код-синхронный).
**Разбор Dome Keeper:** `D:\DomeKeeper_decompiled\DomeKeeper_mechanics_analysis.md` (декомпилят + выводы).
**Создан:** 2026-07-20. **Последняя правка:** 2026-08-11 (одна модель гнома на данж и мир — CrewKit, §2.1).

**Легенда статусов:** ✅ в коде · 🎯 цель (утверждено, не реализовано) · ⛔ похоронено (не возрождать)

---

## 1. Формула игры

> **Скелет Dome Keeper, начинка EBM.** (Финальный маппинг 2026-07-20, вечер.)
> Dome Keeper: купол (защищай+улучшай) + кипер (добывает ради купола и себя). Тугость — из агента, который не может быть в двух местах, и таймера волны, который гонит его домой.

| Кость Dome Keeper | EBM |
|---|---|
| Купол — стационарное сердце, его осаждают | **Город-хаб** (гномье поселение под Хартией ✅): казна, склад, переработка, найм |
| Кипер — уходит добывать, возвращается с грузом | **Башня-персонаж**: выезжает к жилам, **трюм = carry-кап**, возвращается выгрузиться |
| Кипер копает и таскает | **Рука крошит** жилы (пук-пук), **гномы пылесосят** в трюм башни |
| Апгрейды у купола между волнами | **Улучшения в хабе** (верфь-док вешает навороты ✅, кварталы = экономика, найм) + 🎯 срез-стройка нутра башни (вернулась 2026-07-21) |
| «Глубже» = богаче руда + злее волны | **Дальше от хаба** = жирнее жилы/данжи; **следующий хаб** = следующий «слой» |
| Таймер волны гонит домой | **Ночная осада на хаб** — башня должна быть дома к ночи |
| Реликвия = цель забега | Плата Вратам 900🥉 → мех + финальная осада |

**Жадность DK дословно:** «ещё одну жилу до заката» — башня далеко, трюм почти полон, солнце садится.
**Наше отличие:** кипер DK один и бессмертен; у нас башня-персонаж + **расходуемая артель**. ⭐ С 2026-08-07 гномов НЕЛЬЗЯ КУПИТЬ — их выводят из подземелья, поэтому потеря гнома оплачивается не монетой, а новым походом вниз (§2, §6.2).

---

## 2. Сущности — ЧЕТЫРЕ, финал 2026-07-20

- **Башня = персонаж (кипер).** Мобильное тело игрока (WASD, дэш, таран, парри ✅ `tower.gd`), дерётся, возит груз в трюме. Навороты **вешаются** на неё в хабе (верфь-док: парковка чинит, эллинг/бронцех = кап HP, окно срезов ✅). Улучшения корпуса — ВСЕ в верфи (решение 2026-07-21), «нутро по срезам» = их визуализация в окне срезов ✅; боевой обвес = МАШИНЫ в слотах крыши (§5.В).
  **⭐ Башня едет, потому что в ней сидят гномы (2026-08-08).** См. «пилоты» ниже: без экипажа это здание, а не персонаж.
- **Город = хаб (купол).** Стационарный. Казна, склад, переработка, найм, **все улучшения** — как у DK между волнами. Его осаждают ночью → якорь обороны по построению. Диегетика ✅: гномье поселение + Хартия («под крыло»).
- **⭐⭐ Гномы = ПИЛОТЫ башни и расходник (финал 2026-08-08).** Добывают/пылесосят шматки, воюют в данжах, крутят переработку в хабе, **и они же экипаж башни**. Тратятся (солдат = потраченный гном ✅), гибнут. Кап 7 ✅.
- **Рука = манипулятор игрока, НЕ сущность.** БЬЁТ жилы (стоячие пук-пук / ходячие — шлёп на ходу; сама НЕ таскает — носят гномы), кастует/дэш-формы ✅, командует артелью ✅, решает пазлы ✅, ставит лучника ✅.

### 2.1 ⭐⭐ Гномы = пилоты: посадка и высадка (2026-08-08, юзер: «плясать будем от этого»)

Гном перестал быть юнитом ПРИ башне. **В башню можно сесть и выйти**, и это переключает всё:

- **Экипаж внутри** → ты играешь башней: WASD, дэш, рука, касты. Гномы едут в корпусе.
- **Экипаж снаружи** → башня встала (здание), ты играешь отрядом; штатный пеший слой.
- **Посадка = ПЕРЕКЛЮЧЕНИЕ КОНТРОЛЛЕРОВ ЦЕЛИКОМ ✅**, а не «башне добавили WASD»: вместе с корпусом переезжают рука (мышь), камера-риг (зум/орбита/шейк) и HUD башни — ровно те узлы, что стоят в большом мире. Пеший слой на это время не трогает ни камеру, ни мышь. Высадка снимает всё симметрично.
- **ВИДЖЕТ ЭКИПАЖА ✅** (`scenes/crew_widget.tscn`) ставится в момент посадки — окно в кабину. **ОДИН на весь экипаж**, не карточка на класс: состав живыми цифрами и **ровно две кнопки — «Пересобрать отряд» и «Покинуть башню»**. Больше на нём нет ничего осознанно (юзер: «весь остальной функционал лишний»): режимы, тревога, найм — не про пилотов. Сам виджет ничего не решает — кнопка эмитит сигнал, решает сцена, поэтому узел один на данж и большой мир.
- «Пересобрать отряд» открывает ТОТ ЖЕ экран сбора со слот-пятаками (§6.2) — второго интерфейса состава не заводим.
- Статус: ✅ в интро/данже; ✅ в большом мире (`scripts/crew_controller.gd`, 2026-08-11).

**⭐⭐ ОДНА МОДЕЛЬ ГНОМА НА ОБА ЭПИЗОДА (2026-08-11, юзер: «убираем всё, что было до этого о гномах, и ставим всё, что в данже — включая модели»).**

Прежний большой мир рождал ДРУГИХ гномов: гражданских рабочих лагеря (каталожная скорость ~2.2, рабочий AI «ищи дерево / прячься в здание», эскорт за башней, карточка отряда в HUD). Поверх них лежал контроллер экипажа — и у одних тел оказывалось два хозяина: один вёл строй, второй уводил гнома работать. Это и была «бракованная залупа».

- **`scripts/crew_kit.gd` — ЕДИНСТВЕННАЯ точка рождения гнома ✅.** Сцена из каталога, статы отряда, модель класса (блочные артельщик/огневик), кнопка по умолчанию. Данж (`_create_wasd_soldier`) и мир (`CrewController._spawn_crew`) зовут её и НИЧЕГО не досочиняют. Меняешь модель или скорость — меняются оба эпизода разом.
- **Гном мира всегда в режиме экипажа ✅:** рабочей логики лагеря у него нет и включаться она не должна ни на кадр.
- **Ход отряда — данжевые числа ✅** (10 м/с точка строя, тела 8 со спринтом, якорь тела вместо центроида). Замер в мире: крейсер 7.5 м/с при разбросе строя 1.0 м (тела идут 10–13, разница — обход построек у башни).
- **ОДИН ХОЗЯИН ВВОДА ✅:** экипаж вышел — рука башни спит (иначе ЛКМ делает два дела разом). Вернулся — рука просыпается.
- **ЭКРАН ПЕРЕКЛЮЧАЕТСЯ ВМЕСТЕ С УПРАВЛЕНИЕМ ✅** (2026-08-11). Посадка меняет не только руки, но и НАБОР ГЛАГОЛОВ — значит и HUD. Снаружи гаснут трей заклинаний, мана и супер, палитра стройки: это инструменты Ладьи, и висеть они не должны — интерфейс не имеет права обещать то, чего кнопка не сделает. Остаются HP башни (её ломают и припаркованной), ресурсы, часы, квест, журнал. Виджет экипажа виден ВСЕГДА и сам меняет лицо: «ЭКИПАЖ БАШНИ» + «Покинуть башню»/«Пересобрать» ↔ «ОТРЯД В ПОЛЕ» + «Вернуться в башню [E]» и строка кнопок с живым откатом удара. Второго окна «панель отряда» не заводим — это тот же экипаж, просто снаружи.
- **Кнопки отряда в мире:** ЛКМ — полив лучников по курсору ✅, ПРОБЕЛ — удар копейщиков вокруг строя ✅ (урон ×число копейщиков). 🎯 ПКМ (каменный вал артели) пока только в данже: у него своя физика гребня и эхо-волна, порт отдельным шагом. Артель в мир штатно и не приезжает, так что кнопка молчит, а не врёт.
- **Стройка/найм гномами больше не оплачиваются ✅:** производство от гномов отвязано (§9.3), найм в казарме не съедает рабочего — иначе казарма молча перестала бы нанимать. Военная экономика вернётся с переделкой найма (§9.3.7).

### 2.2 ⭐⭐ Откуда берутся гномы — ТОЛЬКО подземелье (2026-08-07)

Юзер: «твои гномы из данжа — это гномы в башне». **Артель-магазин выпилен ✅**: кошелёк рабочих рук не делает, ветка «нанять артель за 5🥈» убрана из диалогов, гейт на продажу рабочих стоит одной точкой в спавнере.

- **Отряд, выведенный из данжа, И ЕСТЬ население башни ✅.** Состав переезжает между сценами (`MatchConfig.next_squad` — тот же автолоад, которым StartMenu уже передаёт состояние следующей сцене).
- **Класс едет вместе с гномом и НЕ меняется ✅** (решение юзера: переучивать нельзя). Привёл трёх лучников и ноль рабочих — шахта стоит. Собранный слот-кнопками отряд определяет не только бой в данже, но и рабочую силу города.
- **Погиб в данже — не доедет:** список собирается по живым на момент выезда. Каждая смерть внизу стоит смены на производстве наверху.
- **Клапан при потере отряда — новый заход в данж** (выбор юзера; аварийный найм за золото ⛔ отвергнут — два крана вместо одного). 🎯 Вход в данж из большого мира ещё не сделан.
- Стартовый состав при прямом запуске мира — ФОЛБЭК, не игровой путь: `level_rooms` главная сцена, открывать её пустой нельзя. С 2026-08-11 это **тот же отряд, что в интро** (3 лучника + 2 копейщика, экспорт `CrewController.default_*`), а не семь рабочих: мир должен открываться теми же гномами, какими его проходят.

**Тугость (ядро):** днём — жадный выезд (как далеко от хаба, как полон трюм, сколько гномов рискнуть); ночью — оборона хаба тем, что успел навесить и расставить. Поверх этого — **выбор «кто в кабине, а кто в поле»**: гном, ведущий башню, не стоит на смене в плавильне.

---

## 3. Луп

### 3.1 Микро (секунды) — рука жонглирует
Мир: пук-пук жилу / ткнуть артель / поставить карту. Бой: каст / дэш-форма / передвинуть лучника / заткнуть прорыв. Пока рука в жиле — она не обороняет; DK-вычитание на внимании.

### 3.2 Мезо (день/ночь) — качели «выезд ↔ осада»
- **День (выезд):** башня выезжает от хаба к жилам → рука крошит → гномы пылесосят в трюм → трюм полон/солнце садится → домой → выгрузка в склад хаба → переработка → **улучшения и расстановка обороны** (вся setup-логистика — ДНЁМ). Бродячие банды = лёгкое давление в поле (✅ WaveDirector, день).
- **Ночь (осада хаба):** скелеты штурмуют ХАБ (✅ мелей у стен + рэйндж-залпы группами, дуга-тревога на банду). Башня обязана быть дома — обороняет тем, что навесила и расставила. **Рука ночью только воюет** (касты/дэш-формы); никакого микро-менеджмента — всё расставлено днём. Затяжная осада = экономическое кровотечение — уточнение причинности (в коде тревога V: смены ОСТАЮТСЯ в зданиях, прячутся только свободные ✅): переработка встаёт, когда игрок САМ снял гномов на оборону или осада ломает здания — кровотечение это выбор/урон, не автоматика. 🎯
- DK-уроки для волны (🎯, см. разбор §3): **weight-бюджет** вместо N врагов; сложность = f(прогресс, богатство игрока); `minRunWeight`-гейт вводит новые типы скелетов плавно; **rubber-band** по урону хабу; **анти-стойл эскалация** против кайта; два флага фазы (`wavepresent`/`wavebattle`); DiveWarner-телеграф по вектору атаки.

### 3.3 Макро (забег) — от хаба к хабу до Врат
Анфилада комнат (✅ rooms-геймлуп, туториал Room1–5, §5.22/5.26). Радиус выезда от хаба = ось жадности дня (дальше = жирнее жилы/данжи, дольше дорога домой). Выработал регион → **переход к следующему поселению-хабу = «спуск на слой глубже»** 🎯; по пути артефакты (Рецепт→кафедра огня, Аккумулятор→молния+шквал, Клад→+300🥉 — сюжет §5.28 ✅ утверждён). Дверные пазлы гейтят (✅ диод/провод/рычаг/мост-доска). Кульминация: **плата Вратам 900🥉 будит меха** (соло-apex, строго 1) + осаду со всех сторон (§5.28).

### 3.4 Мета (между забегами) ✅
Свитки → спеллы навсегда; чертежи → карты в колоду насовсем (`user://tower_profile.cfg`); кафедры = бафы пока стоят. = DK `unlockedElements`; кандидат на добор — их `gadgetToKeep` (перенос одного предмета в след. забег).

---

## 4. Экономика — труба капов (🎯 главный недострой)

**Проблема:** сейчас база = кран (авто-капание) → рисковать нечем. DK-урок: экономика = **пропускная труба**, каждый апгрейд расширяет одно горлышко.

**Типы жил:**
- **Стоячие** ✅ v1 2026-07-21 (`vein_boulder.gd`, `VeinBoulder` в level_rooms ×3 у хаба) — рука пук-пукает на месте (любой урон копится, каждые `damage_per_chunk` выбивает шматок-[ResourceOrb]) → шматки лежат → пока собирает МАГНИТ БАШНИ (орб сам летит в трюм, когда башня рядом и есть место; артель-пылесос = стадия 2). Визуал = общий язык залежей (`VeinVisuals`, тот же что маркеры грида). Исчерпана → рассыпается shatter'ом.
- **Ходячие** (часть жил ИЗНАЧАЛЬНО живые, бродят по полю) — рука должна **шлёпнуть на ходу**, чтобы расколоть → высыпается ресурс (лежит) → гномы собирают. Не пришлёпнул вовремя — **жила ушла, ресурс потерян** (чистая ставка на реакцию; просто уходит, без погони/пасфайндинга). Обычно жирнее стоячих (риск-ревард).

**Разделение труда — ПО СОСТОЯНИЮ цели, без хардкод-фильтра:** движется → **бьёт РУКА**; лежит → **несёт АРТЕЛЬ**. Рука вообще не таскает (отменяет прежнее «рука несёт один шматок»), гномы не гоняются за движущимся. Даёт руке непрерывную скилл-работу в добыче («не крякнул и стой») и DK-вычитание «пока бью жилы — не обороняю».
Переиспользование: ходячая жила = walk-AI скелета ✅; смак = импакт-слой §5.21 ✅; высыпавшийся ресурс + сбор гномами = ✅.

**Целевой поток:** жила (стоячая/ходячая) → **(1) рука бьёт** (пук-пук / шлёп по ходячей, быстро — нарочно обгоняет трубу) → лежачий ресурс → **(2) артель пылесосит** в трюм башни — кап: `свободные гномы × несёт за ходку × скорость` → **(3) трюм башни** = carry-кап кипера (ось прокачки в верфи) → **(4) выгрузка в склад хаба** (кап хранения ✅) → **(5) переработка в хабе** гномами в монеты/ману (гном-в-здании ✅ модель есть).

- **Рассинхронить капы** (крош > сбор > переработка): перекос = жонглирование; ровная труба = скучный кран.
- **Сырьё на земле тлеет** (таймер на дроп) = «не руби больше, чем унесёшь».
- Трюк шреддера DK: больше завал у станции → быстрее переработка (`coolDown = 1/level`).
- Тензия сбора: выгнал артель в поле = не на станциях, не на стене, уязвимы.
- **Валюта ✅:** единая монета, одометр 1🥇=10🥈=10🥉, цены одним номиналом. PAGE/FOOD ⛔ мертвы. Сырьё — вход переработки, НЕ «еда гномам».
- **Сейчас в коде** (замещается): горшки → монеты (✅ `pot.gd`), брёвна → казна +4🥉 (✅ `soldier_gnome.gd`).
- Прототипировать **стадию 2 первой** (пропускная артели) — она даёт весь фил.

---

## 5. Стройка и улучшения — ПЕРЕСБОРКА 2026-07-21 (🎯 новый канон)

**Правило (из разбора DK):** здание = уникальный ГЛАГОЛ · число = узел ветки-прокачки · ресурс добывает только ИГРОК. Тетромино-раскладка остаётся ядром фана: два слоя конкурируют за конечные клетки грида — периметр обороны против роста ядер пристройками.

### 5.А Ядра — ТРИ здания (замок ⛔ вырезан, якорь города = Верфь)
- **Верфь (2×2)** — ЦЕНТР и якорь грида, дом башни, ночная осада идёт на неё. Парковка чинит ✅. ВСЕ улучшения башни здесь: кап HP, броня, трюм, **+слот крыши** (главный дефицит). ⛔ Найм артели отсюда УБРАН (2026-08-07, §2.2) — гномов не покупают, их выводят из подземелья; верфь остаётся кровом при тревоге. «Нутро башни по срезам» = визуализация этих улучшений в окне срезов ✅ — НЕ вторая стройка.
- **Машинный цех (Г, 3 кл.)** — экс-институт: куёт БОЕВЫЕ МАШИНЫ по чертежам, продаёт узлы их веток, льёт энергию (экс-мана, механика та же ✅).
- **Плавильня (Г, 3 кл.)** — приёмник трубы (стадия 4, §4): гном плавит привезённое башней сырьё в монеты. Шахта-кран ⛔.

### 5.Б Ветки ядер = ПРИСТРОЙКИ-полимино 🎯
Узел ветки — не строчка меню, а фигура: купил кликом по ядру → втиснул в его зону-соседство (реюз сапорт-плейсмента ✅). Принадлежит ядру и дереву (зависимости, растущая цена, repeatable = ещё одна такая же). Видов мало, визуальный язык родителя (печь у плавильни, эллинг у верфи, котёл у цеха). Жирные пристройки берут гнома-смену ✅ (тугость артели). Ломаются осадой → узел гаснет до отстройки. Данные веток — decl-каталог (F5/DK upgrades.yaml).

### 5.В Машины на крыше 🎯 («магия = машины гномов»)
- Куются в цеху, **рука ставит в слот крыши** (глагол доказан: гарпун ✅, лучник ✅). Слотов мало: старт 1 → кап ~3 (верфь). «Полные трусы заклинаний» невозможны структурно.
- **Боевые = рука-оператор** (машина в слоте ДАЁТ руке глагол: ПКМ-каст, обводка, дэш-форма; снял — забыл): Огнемётная машина (фаербол), Шквал-орга́н, Мороз-конденсатор, Мино-раскидыватель, Гарпунная турель ✅. У каждой — своя ветка в цеху.
- **Утилитарные = поставил-и-забыл**: Гнездо лучника ✅ (XP-прокачка как есть).
- **Искра — врождённый глагол руки, НЕ машина** (ключ пазлов: диоды/станок — прохождение не должно ломаться лоадаутом).
- Кафедры огня/льда ⛔ как здания → их содержимое = верхние узлы веток машин.

### 5.Г Оборона (главный тетрис) — как есть ✅
Стенка 3/1, Ворота, Колья, Казарма лучников (L-угол, гарнизон+«В башню»), Казарма копейщиков (стеновой отрезок). Ветки казарм: +боец (экс-барак), урон. + Мостки (утилита, вне грида) ✅.

### 5.Д Колода → БИБЛИОТЕКА чертежей 🎯
Рука карт/реролл за ману ⛔ (рандом-ограничитель вырождается: его работу делают дерево+цены+клетки+слоты). Чертёж = ПОСТОЯННАЯ позиция ассортимента (машина в цеху, пристройка, оборона) — DK `unlockedElements`. Мета `tower_profile.cfg` ✅ живёт без изменений, меняется витрина: полки цеха/верфи вместо руки.

### 5.Е План переработки — ✅ v1 ОТГРУЖЕН ЦЕЛИКОМ 2026-07-21 (все 5 этапов)
1. ✅ **Кран→труба:** шахта не капает (тик/гном-спрос выключены), склад башни буферит сырьё (без мгновенной конверсии), плавильня-ядро плавит его в монеты гномом (`_tick_smelter`, камень 5🥉/железо 12🥉, попапы у плавильни).
2. ✅ **Ядра/якорь:** верфь = якорь грида (`CityGrid.core_node`, первая ставится свободно) и цель ночной осады; замок — легаси-резерв (чертёж Заставы жив, но ядром не является); институт → Машинный цех (Г-форма).
3. ✅ **Ветки-пристройки:** узлы = полимино с `attach_to` (гейт зоны в HandPlaceAim._touches_core_zone); окна ядер: верфь (эллинг/бронцех/+слот крыши/найм артели), плавильня (двор ×2 номинал / дом ×1.5 темп + статус трюма), цех (кристалл/руна), казарма (барак +1). Растущие цены/зависимости — 🎯 добор.
4. ✅ **Машины:** `war_machine.gd` (Огнемёт/Шквал-орга́н/Мороз-конденсатор), ковка в окне цеха (+гарпун и минный заряд переехали из мастерской), монтаж рукой на крышу (паттерн гарпуна, поворот за рукой), слоты общие (`ROOF_GROUP`, старт 1 → кап 3 узлом верфи), машина в слоте = SpellSystem.unlock, снял = lock; свитки машинных спеллов = знание ковки (PlayerProfile), Искра врождённая ✅.
5. ✅ **Библиотека:** рука/реролл/колода выпилены из HUD; палитра-меню секциями (ядра/оборона/библиотека чертежей/инженерия), всё за монеты; чертежи профиля = постоянные карточки.
⚠ Хвосты v1: кафедры-школы ещё живы кодом (доступны только чертежом) — их узлы в ветки машин не перенесены; жилы-клетки грида (OilDeposit) остались блокерами-декором; туториал-тексты Заставы упоминают замок.

**Вылетело из палитры** (записи-легаси в каталоге до чистки): дом гномов, чеканный двор, эллинг/бронцех/мастерская (→ узлы), кристалл/руна/кафедра свитков (→ цех), барак (→ узел казармы), кафедры школ (→ узлы машин), шахта, замок, банк/склад/линия/буры/трубы/сторожевые.

---

## 6. Бой (✅ богаче DK — не трогаем, только заостряем)

- **Башня:** дэш/супер-рывок (✅ синий визуал, кламп ~11.5м, к курсору), парирование Q, таран, взрыв при гибели.
- **Дэш-формы заклинаний ✅ (§5.29, «одно из важнейших»):** обводка фигуры заряжает супердэш (фаербол 2 точки/взрыв+горение; шквал пентаграмма/залп); фигура опциональна — ПКМ без фигуры = обычный супердэш.
- **Лучник = оружие башни ✅ (§5.31):** рука ставит любого живого лучника на крышу (1 слот), VS-автострельба, XP → +урон/+темп. DK-урок: авто-турели у них дополняют РУЧНОЕ главное оружие → наш ручной слой = спеллы/дэш-формы руки, не давать бою стать пассивным.
- **Отряды ✅:** лучники-гарнизон, копейщики = стена-гарнизон (прямая 3 кл., колят с хода), F-призыв за башню, рабочие = утилита (не воюют).
- **Враги ✅:** скелеты (мелей-гибрид у стен: обход ≤2×/ломают; рэйндж AOE-залпами по 4 с общим рингом), гигант-босс, мех = соло-apex дуэль/страж Врат. FX-контракт: телеграф/hit-flash/scale-punch/shatter обязательны всем новым врагам.
- 🎯 к добору из DK: стан как отдельный вектор контроля (их `fullStunAt`), пайплайн урона щит→снижение→HP→шипы как модификаторы данных.

### 6.1 Импакт — фичи улучшения (разбор HighFleet, 2026-07-21)
Что УЖЕ зрело (не трогать, не «изобретать заново»): hitstop per-enemy (`hit_stop.gd`, только боссы), tilt/squash (`hit_tilt.gd`), hit-flash (`hit_flash.gd`), отдача башни на выстрел (§5.21.3), trauma-тряска с зум-инвариансом (`camera_rig.gd`), shatter (`shatter_effect.gd`), knockback с resistance (`knockback_state.gd`), burn (`burn_patch.gd`). Урок HighFleet: выстрел = импульс в физ-тело → ломает КОНКРЕТНЫЙ кусок → рвёт связь → поджигает соседей → трясёт экран отдельным тюнимым слоем. Отсюда фичи (статус: ✅ есть / 🔌 есть, не подключено / 🎯 новое):

- **F1 — Направленный shatter по вектору удара 🔌→частично ✅ (2026-07-21).** Механизм ЕСТЬ (`ShatterEffect.spawn burst_dir/power`). СЛЭМ теперь передаёт `hit_dir` в `try_damage` → скелетий death-shatter и жила (веер осколков-шматков конусом ОТ руки + направленное рассыпание) направленные. Осталось: прокинуть hit_dir из fireball/firestorm/дэша в их try_damage-вызовы.
- **F2 — Единая шкала «impact_force» 🎯 (частично ✅).** Сейчас разрозненно: shake ∝ типу снаряда (фаербол 0.2/шквал 0.18/супер), knockback ∝ resistance, а recoil-kick ФИКС (0.22м). Свести в одну силу удара, от которой едут recoil + shake + `power` осколков. HighFleet: всё от массы стрелка/цели.
- **F3 — Локальный урон + отстрел частей для КРУПНЫХ 🎯.** Скелетам не надо (collider=damageable, одиночные). Мех/гигант/**башня** — модульные: под-части с HP, отстрел (ствол/нога/навес) → часть отваливается через F1. HighFleet: дерево Body+Joint. Для башни ложится на навороты верфи (каждый навес = повреждаемая часть). Крупная работа — заложить архитектурно, делать после трубы.
- **F4 — Огонь ползёт + детонация 🎯 (база ✅ `burn_patch`).** HighFleet `m_burn_hp` = порог + распространение. Шаг 1: горящий враг поджигает вплотную стоящего соседа (кормит AOE-фантазию шквала/фаербола). Шаг 2: «взрывоопасные» цели (бочка/погреб/носитель) детонируют цепью. Скоуп держать узким.
- **F5 — Импакт-дайлы → в ДАННЫЕ, не константы 🎯.** Твой принцип «твикать в редакторе» × DK data-driven × HighFleet `values.txt`. Собрать `MAX_TILT_DEG`/`_RECOIL_KICK`/shake-пресеты/shatter-импульсы в один `ImpactProfile.tres` — крутить фидбек без кода.
- **F6 — Ударная волна-кольцо на тяжёлых событиях ✅→расширить.** Шейдер есть (`slam_distortion.gdshader` — ripple+хроматика, уже на слэме руки). Триггерить на редких пиках: супер, топот меха, смерть башни/гиганта. HighFleet: Shockwave + Heat-haze как пост-слой.

Приоритет: **F1 (частично ✅ 2026-07-21: слэм передаёт hit_dir; осталось из спеллов/дэша) → F5 (выносит тюнинг в данные) → F2 → F4 шаг1 → F6 → F3 (крупное; с возвратом срез-стройки §5 модульная башня = та же архитектура — проектировать вместе).** F1/F2/F5/F6 почти целиком переиспользование; новьё по сути F3 и F4.

### 6.2 Пещера: данж-отряд гномов — ТРИ модели управления (песочницы 2026-07-23…28)

**Зачем.** Фан «игры за гномов» не в RTS-микро (солдат-дубль игрока на поле = балласт): гном ценен тем, чего нет у руки и башни — мал (проёмы/данжи, башня геометрически не пролезает), автономен, смертен-как-валюта. Данж-эпизод = смена роли: автошутер отрядом с управлением мышью. Анти-VS: у героя VS одно решение (позиция) — у нас поверх рулёжки решения «стоять/ехать», «дрифт/грип», «газ/тормоз».

**Подход к тестам.** Один скрипт `scripts/dungeon_sandbox.gd` + ТРИ сцены-копии, отличаются только флагом на корне: `dungeon_sandbox.tscn` (модель 1 «тачка»), `dungeon_superhot.tscn` (`superhot_mode` — модель 2), `dungeon_wasd.tscn` (`wasd_mode` — модель 3). Модели живут ПАРАЛЛЕЛЬНО — сравниваем в игре, а не в споре; ветки в коде независимы, итерация одной не ломает другие. Все ручки — exports на корне сцены (твик в редакторе: F6 + Remote-таб, без правки кода). Юниты штатные (ArcherSoldier/Squad/Skeleton; новый класс только ArtelShield-щит), всё новое поведение — АДДИТИВНЫЕ поля с дефолтом «выкл» (`fire_suppressed`/`hold_ground`/`face_override`/`punch_speed`/`quiver_max=0`/`stamina_max=0`) → основная игра не затронута. Для ревью команды — отдельные exe-билды уровня (`D:\Hasan\Blevolord_Dungeon.exe` / `Blevolord_Superhot.exe`; подмена main_scene при экспорте). Общая арена всех моделей: анфилада 3 комнат (волны у стен активной комнаты; **лимит 5 волн на комнату** ✅ 2026-07-28 — отбил → «Волны отбиты ✓», дверь открывается штатным пазлом; в WASD волны заменены **директором давления**, см. ниже), ловушки Room2 (лёд-острова/шипы), грузы, дверные пазлы, крафт бомбы. Читы: R рестарт, T волна (пробивает лимит), E груз.

**Модель 1 — «тачка» (дрифт-инерция, ✅ финал 2026-07-23):**
- **ЛКМ (держать) = газ и руль.** Отряд течёт за курсором-поводком (кламп 7м → 10м с разгоном — запас на замах). Отпустил — ЮЗ по инерции (докатываются, стреляя), ёж собирается на месте остановки. Нажал снова — юз мгновенно перебивается: ЛКМ = педаль.
- **Разгон** = только скорость (3.5с непрерывного бега → +70%), сцепление НЕ трогает. Базовый руль слегка плывёт (grip 5.5) — контроль добирается тормозом.
- **Дрифт = СОБЫТИЕ, не состояние-фейд:** на скорости резкий увод точки (угол ≥55°) → весь отряд СИНХРОННО в скольжение («одна машинка»): сцепление разом падает (5.5→2.6), отдача пылью, полный наклон, линия горит; дуга довернулась ≤18° → синхронный выход. Гистерезис держит дугу.
- **Карв с трением:** в скольжении направление поворачивается, величина сходится к желаемой (scrub 1.6) — момент живёт (орбиты вокруг целей БЕЗ доп. кнопок), разлёта нет. **Сцепка строя** (pull, кап скорости обязателен) — куча дрифтит пачкой, не напарывается на ловушки.
- **ПКМ = тормоз/ручник:** ход и разгон стекают вместе, руление живёт (ручник-поворот = вход в дрифт с тормозом), осаживание корпусом, пыль.
- **Бой из движения:** стоя — «черепаха» (плотный блок-грид рядами поперёк последнего курса, конусы наружу); на бегу — огонь только вперёд по курсу; захват цели: зацепил конусом → держит на 360° в attack_range (орбита-полив). Выстрел не стопит и не сбивает разгон.
- **Груз (E):** командный предмет с классом (`need` = сколько гномов несут ОДНОВРЕМЕННО; куб растёт с классом). E цепляет ближайший — ближайшие N гномов становятся носильщиками (НЕ стреляют — руки заняты; ×0.8 скорость); мало гномов → красный пульс «нельзя». Груз жёстко висит над центроидом носильщиков (буквально в руках). Павшего носильщика сменяет свободный; рук < need → падает. E/падение → RigidBody размораживается и ЛЕТИТ физикой с наследием скорости носильщиков (финт «швырнул груз через опасную зону, освободил стволы, догнал»). **Петля жадности:** большой груз = −5 стволов и −20% хода, тащить через волну страшно по-настоящему.
- **Читаемость:** наклон корпуса = скольжение/разгон, пыль = «визг шин» (только дрифт/тормоз), линия поводка наливается цветом, хвост кометы на бегу ↔ черепаха на стопе (сглаженный разворот).

**Модель 2 — «Superhot-пошаговый» (✅ 2026-07-24, отдана на ревью команде):** мир по умолчанию ЗАМЕР (time_scale → 0.02), оживает когда действуешь; управление дискретное, «шахматы на инерции».
- **Поводок = «длина действия», всегда виден:** и дальность шага, и радиус залпа — одна величина. **Груз укорачивает поводок** (вес 1/3/5 → −метры до пола 2.5м): жадность буквально режет опции хода и огня.
- **Клик в пустоту = ШАГ:** отряд идёт к точке (мир оживает), дошёл → стоп-кадр. По дороге молчит.
- **Клик по врагу под прицелом = ЗАЛП С МЕСТА** ровно по нему (0.7 world-сек, фокус-огонь всем отрядом). Вне залпа отряд НЕ стреляет вовсе — «сам увидел — сам палит» выключено, каждый выстрел = решение игрока.
- **Прицел = точка-снап** у конца поводка (радиус прилипания 1.6м), не сектор: в окружении красится только тот, в кого целишь.
- Техника заморозки: клики ловятся событиями, контрол/прицел/камера — на рендер-частоте с настенными часами (при time_scale 0.02 физтики падают до ~1.2 Гц — поллинг в физике жуёт клики, камера слайд-шоу).

**Модель 3 — «WASD twin-stick» — ⭐⭐ ВЫБРАНА ФИНАЛЬНОЙ (2026-07-29, юзер: «нам всё нравится, походу мы нашли этот ключик и остановимся на WASD»):** поиск управления данж-отрядом ЗАКРЫТ. Модели 1 («тачка») и 2 («Superhot») — архив: код и сцены живы как референс, но не развиваются; при правках общего `dungeon_sandbox.gd` их достаточно не ломать. Дальше данж-эпизод строится только на этой модели (следующий большой шаг — интеграция песочницы в rooms-геймлуп, сейчас она вне его). Хэндофф для UE-команды: `docs/handoff_ue5_dungeon_wasd.md`.
- **Плотная СМЕШАННАЯ группа = тело игрока:** 2 копейщика (передний ряд черепахи) + 3 лучника + 1 артельщик. WASD — движение, курсор — направление огня; вся группа разворачивается к прицелу (пятятся, стреляя на отходе). **Отход спиной стоит темпа** ✅ 2026-07-28: множитель по dot(ход, прицел) — вперёд ×1, задом ×0.55, вбок ровно между; тормозится ТОЧКА СТРОЯ, не юниты (группа = одно тело, строй не растягивается). Кайт перестал быть бесплатным, разворот стал решением.
- **⭐ ЖЕЛЕ-СТРОЙ ✅ 2026-07-28** («движение как единый организм, слегка желеобразный»): четыре слоя, все — деформация СЛОТОВ (`Squad.jelly_*`, дефолты нейтральны → основная игра не тронута), движение юнитов не переписано. **(1) Пружина инерции:** ускорение толкает строй против хода — рванул, тело отстало; встал, накатилось вперёд и качнулось (замерено ~14 см отставания / ~10 см наката при шаге строя 75 см). **(2) Вытяжение:** на скорости строй тянется вдоль курса (+35%) и жмётся поперёк (−16%) — капля; на стопе снова круглый. **(3) Волна:** задние ряды отстают сильнее передних. **(4) Перетекание:** голова рулит жёстко, хвост вязче (grip ×0.45 по индексу) — тело перетекает за поворотом, а не крутится доской. ⚠ Считать от ТОЧКИ СТРОЯ, не от центроида: через центроид выходит обратная связь (слоты двигают гномов → центроид едет → слоты двигают ещё) и строй раскачивает сам себя.
- **Дрожь на отходе спиной ✅ вылечена** (`SoldierGnome.arrival_damp_radius`, 0 = штатное поведение): подход к слоту был бинарным — полный ход до порога 0.4м, потом стоп. Пока строй едет вперёд, слот убегает и всё гладко; на отходе слот НАЕЗЖАЕТ на гнома сзади, и тот попадал в цикл стоп-старт-перелёт. Теперь у слота скорость гасится пропорционально остатку пути.
- **⭐ РАСКЛАДКА-ФИНАЛ (пивот 2026-07-28, идея юзера):** мили-класс сжат в **ПРОБЕЛ**, фокус-группы 1/2/3 и клик-панч ⛔ выпилены: они существовали только чтобы выдавать приказы разным отрядам — когда приказ один, меню становится пустым интерфейсом («жму цифру, чтобы получить право нажать кнопку»). Один жест вместо режима. **Лучники стреляют КАК БЫЛИ — по зажатому ЛКМ** (уточнение юзера: гашетка остаётся в руках игрока; авто-огонь ⛔ по-прежнему). Снят только гейт фокуса: лучники всегда в боевом режиме, выбирать их цифрой больше не нужно. Копейщики между суперами не молчат: автоукол вплотную живёт всегда, иначе передний ряд станет декорацией, а он телом держит толпу. Решения в секунду: куда идти, куда лить, когда жать.
- **⭐ СОСТАВ = НАПОЛНЕНИЕ КНОПОК, экран = СЛОТ-ПЯТАКИ ✅ финал 2026-08-06** (три итерации за два дня, все три предыдущие юзер отверг — история ниже). Сбор перед заходом и перестройка в походе — ОДИН экран в **ОТДЕЛЬНОЙ КОМНАТЕ-ПРЕДБАННИКЕ** (строится кодом в стороне, по ENTER сносится целиком — реквизит не мозолит глаза в бою), не UI-плашка (фидбек «выбор неудобный, надо тактильный, как у нас всё — рукой»). На полу **ТРИ ПЯТАКА-СЛОТА, и слот = КНОПКА отряда**: `[ПРОБЕЛ] удар вокруг` · `[ЛКМ] полив` · `[ПКМ] вал / залп`. Сбоку кучки классов. Жест: клик по кучке → группа целиком в руку (толпой над курсором), клик по строю → она **сама ложится в слот своей кнопки** — целиться в конкретный пятак не надо, порядок и позиции не значат ничего; ПКМ по слоту — снять группу обратно. **Одна кнопка = один класс СТРУКТУРНО:** кинул огневиков на занятый артелью ПКМ — артель выпрыгнула в свою кучку. Кучки хранят ОЧЕРЕДЬ снятых групп с их живым счётом, поэтому перестановка никого не воскрешает. Кольцо слота красится цветом занявшего класса, пустое серое — «на этой кнопке никого». Тела — ШТАТНЫЕ модели классов (те же сцены, что пойдут в бой), замороженные статуями: ни AI, ни физики, ни коллизий. Сбоку — маленькая панель ПАРАМЕТРОВ, и это не «описание классов», а прямой предпросмотр силы от состава теми же числами, по которым бой считает: залп N стрел и урон за стрелу, удар копейщиков `28×N` с радиусом и откатом, вал артели `45×N` с шириной полосы. Клавиши 1–4 и Shift+цифра — запасной путь, ENTER — в подземелье. Пока экран открыт, мир стоит: ни отряда, ни врагов — состав не выбирают под огнём; в походе экран ещё и замораживает тела (найм огневиков при занятом ПКМ, пункт «Перенастроить отряд» в диалоге любой хижины, чит `J`). **⛔ ОТВЕРГНУТЫЕ ИТЕРАЦИИ (не возрождать):** *(1)* сетка ячеек с посадкой ПО ОДНОМУ гному и липкой рукой (2026-07-29, прожила неделю) — «одинаковые гномы назначаются на одинаковые кнопки», состав не читался как раскладка; *(2)* ряды-зоны по кнопкам с ячейками внутри — та же расстановка по одному, только в три ряда; *(3)* **ПОЛИМИНО-доска** с фигурами отрядов, поворотами R/колесом и призраком (прожила день) — «механика излишняя, они всё равно в куче, расстановка на геймплей не влияет». Урок общий: юзеру нужна не пространственная выразительность, а **честная модель «кнопка ← класс»** с одним тактильным жестом; всё, что заставляет раскладывать единицы по местам, не оплачивая это геймплеем, — налог. **Урон способностей = СУММА по живым носителям** (ответ на вопрос «суммирующий получается урон каждого?» — да, и это правильно): у лучников это и так число стволов, у копейщиков ПРОБЕЛ бьёт `28 × живых копейщиков`, у артели вал — `45 × живых артельщиков`, причём вал считает урон В МОМЕНТ ПОПАДАНИЯ, так что гибель носителя ослабляет его на лету. Балансовая точка: полный расчёт копейщиков (2) сносит щитовика с одного пробела (56 > 45 hp), потерял одного — уже нет. Так потеря гнома бьёт по ВОЗМОЖНОСТЯМ, а не по абстрактному запасу HP, и «гномы = дисплей возможностей» работает в обе стороны. Класса нет в отряде → нет и его кнопки (без копейщиков ПРОБЕЛ молчит, без артели — ПКМ). **Билд без мили РАЗРЕШЁН** (осознанно тяжёлый выбор, не ошибка: щитовиков придётся выедать стрелами через щит), но на сборе это предупреждение прямо в панели — цена билда должна читаться ДО входа, а не выясняться в бою.
- **Состав отряда = билд, без второго слоя карточек-абилок** (юзер: гномы ходят из данжа в открытый мир с башней, где карточный слой уже есть — плодить второй глупо). Дальний бой и артель в группе ВСЕГДА; мили — опционально, и само его наличие открывает ПРОБЕЛ. Нет копейщиков → нет способности.
- **ПРОБЕЛ = удар вокруг ✅ (БЕЗ задержки):** нажал — грохнуло, промежуточного состояния нет (замах 0.4с попробован и снят в тот же день: «пусть без задержки бьёт»; ручка `wasd_super_windup` жива, 0 = мгновенно). Волна от строя: урон + отброс наружу по всем в радиусе 6.5м, `hit_dir` от центра → направленный shatter (§6.1 F1). **Урон держать выше hp щитовика** (55 vs 45): один пробел разваливает ключ-цель наверняка — при 34 связка «вижу ключ-цель → жму кнопку» рвалась ожиданием второго кулдауна и щитовики читались как хардкор. ⛔ Веер `punch_at` убран — вылет-хлыст к цели читался как «копейщики побежали ко врагу»; **они бьют С МЕСТА и строй не покидают**. Кулдаун 6с, готовность — вспышка бордера на карточке копейщиков (кнопка вспыхивает, не полоска). Карточки 1/2/3 остались ДИСПЛЕЕМ состава (живые по типам, гаснут с группой), выбора в них больше нет.
- **ПКМ = КАМЕННАЯ ВОЛНА артели ✅ (симметрия классов, 2026-07-28):** у каждого своя атака — ЛКМ лучники, ПРОБЕЛ копейщики, ПКМ артель. Вал камня уходит от строя ПО ПРИЦЕЛУ (направление фиксируется на запуске — целиться надо заранее, вал не подруливает), сметает всё в полосе ±1.9м (урон 45 + отброс с `hit_dir` по ходу), рассыпается shatter'ом о стену (raycast по TERRAIN/WALL_GATE/CHASM) или выдохшись на 20м. Кулдаун 14с — **заряжается долго намеренно:** это не расходник на каждую стычку, а «расчистить коридор», когда прижали спереди. Щит-плашка заменена ею: плашка была пассивной («поставил и жди»), волна — действие. Готовность обеих способностей — вспышка бордера на карточке класса + отсчёт.
- **Копейщик НЕ ВОЮЕТ САМ ВООБЩЕ** («чтобы ничего не делали кроме пробела»): детект 0 → цель не находится → ни погони, ни укола в упор. Между суперами он тело в строю, вся его работа — одна кнопка. Автоукол пробовали как фон и сняли: любая самодеятельность мили размывает то, ради чего он в отряде.
- **Ключ-цели встали на место:** щитовик (стрелы не берут) теперь причина НАЖАТЬ ПРОБЕЛ, бегун в спину — причина развернуть курсор. Поводы для действия вместо поводов для меню.
- *(историческое, до пивота 28.07)* **[2] Лучники:** зажатый ЛКМ = полив в направлении курсора (баллистика в точку, цель-нода не нужна; скелет на луче лишь уточняет прицел). **[1] Копейщики:** клик = удар-кулак по цели прицела с импакт-пакетом (замах-антиципация 0.06с → вылет-хлыст 28 м/с → хитстоп + искры на контакте → отскок в живой слот строя; цикл 0.43с). **[3] Артель:** ПКМ = ⛔ щит-плашка (заменена волной 2026-07-28, код жив в `_try_place_shield`).
- **Драйвер переключения = КЛЮЧ-ЦЕЛИ ✅ 2026-07-28** (⛔ колчан/стамина ВЫПИЛЕНЫ тем же днём: «очень сбивает теншен и темп»). Разбор провала: и колчан, и стамина — драйверы «ТОЛКАТЬ». Сухой колчан не даёт решение, он даёт ПРОСТОЙ: стоишь и ждёшь, пока цифра натикает, а смена фокуса из манёвра превращается в повинность. Ритм получался, но ценой провалов в действии. Замена — драйвер «ТЯНУТЬ»: переключаюсь, потому что вижу НА ЭКРАНЕ цель, которую текущий инструмент не берёт. **Щитовик** (стрелы бьют не в тело, а в ЩИТ: прочность 45, плита темнеет по остатку и отваливается — дальше обычный скелет; медленный танк hp 45. **Абсолютный щит снят в тот же день:** когда на поле оставались одни щитовики, лучники выключались и игрок бегал кругами в ожидании кулдауна — тот же мёртвый простой, что убил колчан. Правило: ключ-цель меняет ПРИОРИТЕТ инструмента, но никогда не отключает его в ноль) и **бегун** (быстрый хилый, лезет В СПИНУ из тыльного сектора → щит [3] или разворот группы; «стоять и лить вперёд» перестало быть даром). Подаются КУЧЕЙ одного типа (группа щитовиков читается как группа) с гейтом по времени в комнате (12с / 20с = DK `minRunWeight`): первые секунды чистый полив, ключ-цели входят, когда игрок освоился. Оба типа = параметризованный ШТАТНЫЙ Skeleton (статы + одна деталь силуэта декор-ребёнком), новых классов нет. Заодно лечит «группа слишком мощная» без нерфа цифр. Колчан на спине ОСТАЛСЯ как декор (юзер: «сами колчаны визуально смешные») — всегда полный, ничего не гейтит.
- **Подача врага = ДИРЕКТОР ДАВЛЕНИЯ ✅ 2026-07-28** (диагноз «так себе, весело должно быть с первых секунд»). Причина была не в управлении, а в арифметике подачи: 3с пауза + скелет 2.0 м/с идёт от стены 22 м ≈ 11с → первые ~7 секунд игроку нечего делать; 4 скелета с четырёх РАЗНЫХ стен = 12 стрел за 10с, при которых колчан/стамина/щит структурно не включаются (ритм 1↔2 был отлажен, но не нагружен). Замена: одна кривая «сколько врага должно быть в лицо ПРЯМО СЕЙЧАС», директор доливает поштучно-непрерывно до неё — вместо четырёх ручек (`wave_interval`/`base`/`growth`/`room_wave_limit`). = DK-урок weight-бюджета (§3.2) в масштабе комнаты. Три части: **(1) стартовая пачка** — 7 скелетов УЖЕ на арене в t=0 на 9.5 м (внутри дальности лучника: первое действие на первой секунде), двумя кучами; **(2) кривая** 8 → 18 живых за 75с в комнате, долив кучей до 3 шт / 0.9с (передышка = провал кривой, а не мёртвый таймер); **(3) фронтальный спавн** в секторе ±55° от −Z (из глубины анфилады, навстречу ходу) — у группы есть перед и спина, отход и фокус-огонь осмыслены; спавн по случайной из 4 стен убивал twin-stick. Бюджет комнаты (55 скелетов) заменил лимит волн: выдан и добит → «Волны отбиты ✓». Всё в exports корня (`director_*`), модели 1/2 на старых волнах. HUD: «Натиск: живых/кривая · в запасе N». Чит T = внеплановая куча сверх бюджета.
- **⭐ КАРТОЧКИ-НАХОДКИ = кривая роста в забеге ✅ 2026-07-28** (идея юзера, закрывает дыру «нет ощущения VS»). VS-левелапы по опыту отвергнуты юзером с ясной причиной: их пришлось бы тащить на этап с башней — «разные балансы, гномы и прочее, это жесть». Решение: не система прогрессии, а **находки**, и это уже существующий язык игры (свитки → спеллы, чертежи → библиотека, артефакты акта §5.28). Правила: **(1) живут ТОЛЬКО внутри данж-забега** и наружу не уходят — в башню, как и раньше, уезжают ресурсы/свитки/чертежи, поэтому её баланс не трогается вовсе; **(2) вешаются на КАТЕГОРИЮ** (все лучники), не на гнома — иначе микроменеджмент и боль при потерях; **(3) падают за ЗАЧИСТКУ КОМНАТЫ** — выбор из 3 на «Волны отбиты ✓» клавишами 1/2/3 (освободились после выпила фокуса): комната = уровень, три комнаты = три выбора, полоска опыта не нужна; **(4) лимит 3 на категорию** — заполнена, её карты больше не выпадают (дефицит слотов = выбор, DK §7.4); **(5) перевес за ПРАВИЛАМИ, а не процентами** — «+15% чего-нибудь» фила не даёт (проверено на колчане и стамине). **Два источника выдачи ✅ 2026-07-29:** зачистка комнаты + **СУНДУК** в каждой комнате (стоит в стороне от маршрута, по углам вразнобой — за находкой надо свернуть; дошёл отрядом = открыл). Три выбора за забег оказалось мало для ощущения роста, сундуки удваивают их и платят за «полазить», а не за бой. Выдачи, пришедшие при открытом пикере, **встают в очередь** и не теряются (раньше зачистка следующей комнаты с невзятой картой пропадала впустую). Каталог v1 (9 карт, из них 5 правил): лучники — темп +20% / дальность +4м / **щитобой ×3 по плите**; копейщики — радиус +2м / откат −2с / **выжженная земля после удара** (штатный BurnPatch); артель — **+камень с каждой стороны** / **обвал: вал взрывается осколками при ударе** / **эхо: через 0.35с ИЗ СТРОЯ вдогонку первому идёт второй вал — 3 камня, урон вполсилы** (первая версия пускала его из точки, где первый разбился — там он бесполезен; валы теперь живут параллельно, список вместо одного узла). Почти всё — множители к уже написанным ручкам, новых механик не заведено. Набранное видно на карточке класса. Сеты (бонус за 3 из школы) намеренно НЕ делаем: тройка сама по себе сет, слой поверх слоя добавим, только если её окажется мало.
- **⭐ БЕСТИАРИЙ ПО КОМНАТАМ ✅ 2026-08-08** (юзер: «разбавим врагов балансно, по новому типу в каждую комнату»). Каждая комната приносит РОВНО ОДИН новый тип — игрок разбирается с ними по очереди, а не тонет в зоопарке сразу:

  | Комната | Состав | Чему учит |
  |---|---|---|
  | К1 «кишка» | обычные | вести строй; врагов читать пока не надо |
  | К2 «обеденная» | + **щитовик** | стрелы вязнут в плите → причина нажать ПРОБЕЛ |
  | К3 | + **бегун** | хилый спринтер лезет в тыл → причина развернуть курсор |
  | К4 «ледник» | + **бомбист**, ⛔ щитовиков НЕТ | бить издали; на льду несёт юзом прямо на фитиль |

  Ангар (финал) юзер не называл — там пока чистые обычные.
  **Раздача — ПАТТЕРН по кругу, не веса** (`ROOM_PATTERN`): пачка из 4 гарантированно содержит новый тип. На весах при 20% из пятерых мог не выпасть ни один, и «балансно» превращалось бы в лотерею.
  Все типы — параметризованный ШТАТНЫЙ Skeleton (статы + одна деталь силуэта декор-ребёнком), новых классов врагов нет.
  **⛔ Урок терминологии:** «прыгуны» в устах юзера = БЕГУНЫ, уже сделанные. Я прочитал буквально и завёл лишний тип `jumper` с прыжком через строй — вырезан целиком. Незнакомое название врага сперва искать среди готовых.

- **⭐ БОМБИСТ = БОМБА С ФИТИЛЁМ ✅ 2026-08-08** (уточнение юзера — не «рвётся, если убить»):
  подошёл к гному ближе 3.2 м → **зажёгся фитиль (1.6 с)**: заряд краснеет, мигает всё чаще (3→14 Гц) и раздувается — читается боковым зрением, а не только по цвету. Убежать от своего фитиля он не может.
  **Догорел → взрыв. Застрелен С ГОРЯЩИМ фитилём → рвётся на месте. Снят ДО фитиля → взрыва нет вовсе** — в этом вся тактика: заметил на подходе и убрал — чисто; проморгал — выбираешь только место, где рванёт. Взрыв бьёт и своих (симметрия: толпа скелетов вокруг бомбиста — твой шанс).
  Заряд в покое тёмный, красный = строго «сейчас рванёт»; оранжевый остаётся приметой бегуна (один визуальный язык — один смысл).

- **Принцип «нет полосок — гномы показывают возможности»:** ХП = живые гномы (карточка гаснет с группой); состояние читается по телам и силуэтам, а не по шкалам. Колчан на спине остался декором, счётчиком быть перестал; тип врага читается деталью силуэта (щит-плита спереди у щитовика, гребень у бегуна).

**Песочница:** `scenes/dungeon_sandbox.tscn` + `scripts/dungeon_sandbox.gd` — ВНЕ геймлупа rooms; юниты штатные (ArcherSoldier+Squad+Skeleton, новых не заведено; аддитивные exports SoldierGnome с дефолтом 0 = основная игра не тронута). Баланс песочницы: урон лучника 8–12 (~3 выстрела на скелета), волны 4+2N. Грабли тест-сцен: LOD юнитов выключать до add_child (FAR = полёт сквозь стены), комнату держать вдали от мирового нуля (пустой навмеш снапит в (0,0,0)).

**⛔ Отвергнуто в итерациях (не возвращать):** плавный grip-фейд по разгону («дрифт подкрадывается»), верёвочный свинг с доворотом в касательную («противоестественно»), карв без трения (разлёт), резкий стоп на отпускании (сменён юзом с приходом «тачки»), ПКМ-пин (код спит); Superhot: непрерывное «время-от-скорости-газа» (суперседнут пошаговым кликом), конус-прицел dot>0.3 (в окружении поводок вечно красный — заменён точкой-снапом); WASD: hold-штурм копейщиков полным FSM («не импактно и не управляемо — должен быть УДАР» → punch_at), авто-конус лучников «сам увидел — сам палит» (огонь строго по прицелу игрока), **колчан-обойма и стамина копейщиков как ЛИМИТЫ** (2026-07-28: любой драйвер-«толкать», который отключает инструмент, обнуляет темп — переключать должна причина на экране, а не пустой счётчик; ручки живы в exports, дефолт 0).

Плюс к «тачке» (✅ 2026-07-23): **телесность** — гном↔скелет коллизии в песочнице (толпа = стена; в осн. игре выкл ради перфа толп; `Skeleton.extra_collision_mask` аддитивный); **множитель темпа отряда** `gnome_speed_scale` (шаг + спринт догона + буст разом — `move_speed` рулит только шагом у слота); **груз E** (✅ выше).

**⭐ ГНОМ-ОГНЕВИК = четвёртый класс ✅ 2026-08-05** (интро К4, канон комнаты — `docs/scenario_dungeon_intro.md`). Покупается в хижине ледника, садится на кнопку **ПКМ вместо артели** (одна кнопка — один класс, см. экран состава выше). Залп = **мини-фаерболы, уменьшенные копии выстрела башни** (тот же `fireball.tscn`, отличаются только цифры `setup` — новой сущности не заведено) в точку курсора, по снаряду с живого мага. **Урон тремя слоями, чтобы точность решала, а огонь добивал:** прямое попадание 30 ближайшему в метре от взрыва (+ хитстоп), AOE радиусом 2.6 слабый (10 в центре, затухание к краю), горящая земля штатным `BurnPatch` башни (4 / 0.5с, 3с). Огонь **топит лёд** — это ключ комнаты, а не только урон: сегменты шипастых стен и колонны (2 попадания), ледяной завал выхода (4, другими силами не берётся), попадание в пол выжигает **талое пятно** — остров штатного сцепления на катке. Симметрия льда соблюдена: скользит и отряд, и скелеты (у скелета занос ведёт СВОЙ вектор с клампом на `move_speed` — читать `velocity` обратно нельзя, LOD-батчинг множит её на divisor и скорость копится лавиной).

**🎯 Очередь слоёв:** артель-турели для WASD (защитные/атакующие/лечащие — «это следующий шаг»), **TTK вниз / количество вверх** (3 стрелы на скелета дорого; попкорн-закон VS — килл примерно раз в секунду: скелет в 1–2 стрелы, но их втрое больше), **бегун — не «когда-нибудь», а в первую подачу** (его настоящая работа: дать контакт с 5-й секунды → копейщики/стамина/щит включаются, пинг-понг 1↔2 живёт с начала), **сочность полива** (зажатый ЛКМ = 90% времени игрока, а импакт-пакет есть только у панча; `hit_dir` по §6.1 F1 прокинут пока лишь из слэма — стрела бьёт ватно), третья ключ-цель под артель [3] (щитовик зовёт копейщика, бегун — щит/разворот; у артели пока нет СВОЕЙ цели-ключа — кандидат: дальнобойный скелет-стрелок, от которого спасает только плашка), сбитые-vs-смерть (v1 = простая смерть; «сбитые с подбором» — если потери злят), точка сдачи груза (принёс → награда, замкнуть петлю), формации по обводке фигур (язык дэш-форм; черепаха/хвост уже эмерджентная база), интеграция в данжи rooms.

---

## 7. DK-уроки поверх всего (сводка)

1. **Data-driven баланс:** числа врагов/оружия — в YAML/ресурс, код читает из хранилища (их `properties.yaml`/`upgrades.yaml`; апгрейд = строчка `laser.dps += 10`). Прямой ответ на «твикать без кода».
2. Экономика = труба капов (§4). 3. Волны = weight-бюджет + 2 оси + rubber-band + анти-стойл (§3.2). 4. Дефицит слотов купола → слоты нутра башни (§5). 5. `repeatable`-апгрейды с растущей ценой = бесконечный late-game. 6. `gadgetToKeep` → перенос предмета между забегами. 7. Разблокировки через задания (assignments), не только покупку.

---

## 8. Гайдрейлы — ⛔ НЕ возрождать

- Заряды, клик-уровни, стопка-сбор шахты (откат 2026-07-08 «вернуть как было»).
- Пересборка 2026-07-21: качалка-замок (якорь = верфь); здания-краны (шахта-капалка); сапорты-множители как САМОСТОЯТЕЛЬНЫЕ здания палитры (числа живут узлами-пристройками веток ядер); рука карт/реролл за ману.
- Караван/палаточная змейка; город/стройка на земле (пивот 2026-07-20); Док города.
- PAGE/FOOD как валюты; победа-нефть (выключена 2026-07-07, победа = Врата).
- camp_xp (лагерь рос постройками за ресурсы — теперь и это в прошлом вместе с землёй).
- **Покупка гномов за золото** (артель-магазин, 2026-08-07): руки берутся только из подземелья, второго крана не заводить — кошелёк всегда удобнее похода. Аварийный найм «на случай вайпа» тоже ⛔.
- **Переучивание гномов** (рабочий → лучник за деньги): класс приезжает с гномом и не меняется.
- **Лишние кнопки на виджете экипажа** (2026-08-08): только «Пересобрать отряд» и «Покинуть башню».
- Сложное поверх простого: один жест, fire-and-forget, без микро-няньканья. UI 1:1 без stretch; панели — узлами в .tscn.

---

## 9. Открытые вопросы и слабые места

### 9.1 ✅ РЕШЕНО (2026-07-20, вечер): маппинг финализирован
Хаб = купол, башня = кипер, гномы = расходник, рука = манипулятор — вшито в §1–2. История итерации: город-в-башню (день) → локальная база вернулась (вечер) → четыре сущности (финал) → **2026-07-21: срез-стройка внутри башни ВОЗВРАЩЕНА** (сплит «всё в хабе» смягчён: хаб = экономика/найм/верфь-навороты, нутро башни = своя стройка по срезам, границу решить — §9.3.1).

### 9.2 Слабые места лупа — статусы после разбора
1. **Мобильная база подрывает осаду** → ✅ РЕШЕНО пивотом §9.1: осаждают стационарную базу, не башню.
2. **Конечные жилы vs фикс-плата Вратам** → вопрос баланса: жилы вокруг каждой базы покрывают расходы стенда с запасом; кандидат-ручеёк «генератор = золото в кольце 0».
3. **Спираль артели** → снято рефреймом: гномы = расходник (цена, не каскад), пока есть найм в поселениях.
4. **Коллизия глаголов рука/гномы** → ✅ РЕШЕНО чище: делёж ПО СОСТОЯНИЮ (движется→бьёт рука / лежит→несёт артель, §4). Рука не таскает вообще — только бьёт (в т.ч. ходячие жилы). Симметрия цела, фильтра нет.
5. **Перегруз руки в осаде** → ✅ РЕШЕНО: расстановка (артель, лучники, гарнизоны) — ЗАРАНЕЕ, днём; ночью рука только воюет (касты/дэш-формы). Ритм DK: setup между волнами, execution в волне.

### 9.3 Прочее открытое
1. ~~Нутро башни~~ ЗАКРЫТО 2026-07-21: улучшения корпуса = верфь (окно срезов ✅ = визуализация), боевой обвес = машины крыши (§5.В). Открытым осталось: слоты крыши — старт 1 / кап 3 (числа на плейтест).
2. Дэш-формы при машинах: врождённое свойство руки для ЛЮБОЙ установленной боевой машины (рабочая гипотеза) vs отдельный узел в ветке каждой машины (жирная веха ветки). Решить на этапе 4 (§5.Е).
3. Судьба земляных построек-контента (палисад/стены = MELEE_ONLY ✅ живы в бою) — что остаётся полем, что уходит в нутро.
4. Условие победы технически не подключено (MatchGoal читает старый Camp) — канон: Врата 900🥉 (§5.28), довести до кода.
5. Числа трубы (стадия 2): скорость крошки/переноски/тления — на прототип.
6. **🎯 Вход в данж из большого мира** (§2.2): гномов теперь дают только подземелья, а спуск из хаба вниз не сделан — потерял отряд, добрать неоткуда. Решить заодно: что делает хаб и осада, пока игрок внизу.
7. **⚠ Казарма всё ещё лепит лучника из рабочего** (`_consume_population`) — это то самое переучивание, которое §2.2 запрещает, просто через другую дверь. Кандидат-ответ: казарма перестаёт производить и становится ЁМКОСТЬЮ для привезённых лучников (сходится с бараком, он уже «ёмкость»). Не трогал — это переписывает военную экономику.
8. **🎯 Посадка-высадка в большом мире** (§2.1): виджет туда встанет почти даром, но «башня не двигается без пилотов» меняет весь основной геймплей. Открыто: что делает башня без экипажа — просто стоит зданием или продолжает обороняться сама.
9. **⚠ Баг вертикали у врагов** (`Enemy._physics_process`): безусловное `velocity.y = 0` при `is_on_floor()` выполняется ДО `move_and_slide` и убивает ЛЮБОЙ импульс вверх. Молча не работают все вертикали в коде — гигант трижды швыряет скелетов `+ Vector3.UP * 2..3`, пинок аппарата `+ Vector3.UP * 0.5`; автор писал подброс, на экране плоский толчок. Лечится одной строкой (`elif velocity.y <= 0.0`), взрывы не затронет (AoeDamage сам зануляет `dir.y`), НО меняет чувство оттюненного гиганта — нужен отдельный плейтест. Предупреждение оставлено в коде на месте.

---

## 10. Навигация

| Что | Где |
|---|---|
| Старт игры «Из недр — к Ладье» (данж-интро, канон) | `docs/scenario_dungeon_intro.md` |
| Живой код-синхронный геймплей (туториал) | `docs/gameplay.md` |
| Реализация low-level | `SPEC.md` §5.1–5.31 (ниже строки 3217 — легаси, не читать за истину) |
| Сюжет акта «Верхний Предел» | SPEC §5.28 |
| Разбор Dome Keeper + пивот экономики | `D:\DomeKeeper_decompiled\DomeKeeper_mechanics_analysis.md` |
| Ключевые файлы кода | `docs/gameplay.md` §13 |
